name: deployment-workflow.yaml

on:
  push:
    branches:
      - main

jobs:

  deploy:
    runs-on: self-hosted
    steps:
      - name : Hello, World!
        run:
          chmod +x /home/ubuntu/deployment.ch &&
          echo "Hello, World!" >> hello.log
